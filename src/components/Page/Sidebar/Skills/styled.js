import styled from 'styled-components';

export const Container = styled.div`

  .bar-expand {
    display: flex;
    align-items: center;
  }

  .bar-label {
    font: 12px Raleway, sans-serif;
    font-weight: 400;
    letter-spacing: 1px;
    position: static;
    top: 0;
    left: 0;
    padding-left: 10px;
  }
`;
